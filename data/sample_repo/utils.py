"""Utility helpers for the sample repository."""


def format_message(text: str) -> str:
    """Format a greeting message."""
    return f"=== {text} from sample repo ==="


def add_numbers(a: int, b: int) -> int:
    """Return the sum of two integers."""
    return a + b
